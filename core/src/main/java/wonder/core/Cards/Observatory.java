package wonder.core.Cards;

import wonder.core.Card;

public class Observatory extends Card {
    public Observatory(int minPlayers) {
        super("Observatory", Type.Green, Age.Three, minPlayers);
    }
}
